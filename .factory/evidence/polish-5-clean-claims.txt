# Perfection loop 5 — clean-clone claim evidence

Commit: c627fc57d8c7ab1f6ab561901516940693b55133
Clone: /tmp/restore-drill-polish5-clean.UgMCAu/repo
Install: npm ci — PASS, 0 vulnerabilities

Each exact `.factory/claims.json` command was run separately:

PASS @claim:sample-demo
PASS @claim:browser-privacy
PASS @claim:demo-no-persistence
PASS @claim:install-from-site
PASS @claim:free-cli
PASS @claim:isolated-container
PASS @claim:data-tmpfs-size
PASS @claim:backup-local
PASS @claim:newer-version
PASS @claim:signed-receipt
PASS @claim:default-signing-key
PASS @claim:schema-readiness
PASS @claim:cli-demo
PASS @claim:podman-runtime
PASS @claim:dump-formats
PASS @claim:json-output
PASS @claim:ci-mode
PASS @claim:no-production-url
PASS @claim:no-telemetry

The repaired install test resolves `refs/heads/main` from the public GitHub URL,
checks out that exact SHA in a fresh directory, runs the published locked Cargo
install command, and invokes `restore-drill --help` from the installed prefix.

Full clean-clone gates:

- `npm test` — PASS: 5 Rust tests and 32 Playwright tests.
- `npm run build` — PASS: release binary and `dist/site/` produced.
- `cargo fmt --check` — PASS.
- `cargo clippy --all-targets -- -D warnings` — PASS.
- `cargo package --no-verify` — PASS: `target/package/restore-drill-0.1.0.crate`.
- `npm audit --omit=dev --audit-level=high` — PASS: 0 vulnerabilities.
- `git diff --check` — PASS.
