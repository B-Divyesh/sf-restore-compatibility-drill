[
  {
    "id": "sample-demo",
    "claim": "The browser demo replays the bundled sample to a signed pass result.",
    "where": "hero action and demo",
    "test": "npm test -- --grep @claim:sample-demo",
    "sandbox": "fresh Chromium context; open the demo and run the bundled sample"
  },
  {
    "id": "browser-privacy",
    "claim": "The site and browser demo make only same-origin requests and load no analytics, advertising scripts, or third-party fonts.",
    "where": "demo and privacy page",
    "test": "npm test -- --grep @claim:browser-privacy",
    "sandbox": "fresh Chromium context; record every request and resource type during the full demo flow"
  },
  {
    "id": "demo-no-persistence",
    "claim": "The browser replay does not save sample data and Reset or reload returns it to its first frame.",
    "where": "demo banner, demo introduction, and privacy page",
    "test": "npm test -- --grep @claim:demo-no-persistence",
    "sandbox": "fresh Chromium context; run, reset, and reload; inspect localStorage, sessionStorage, IndexedDB, and OPFS"
  },
  {
    "id": "install-from-site",
    "claim": "The public install instructions produce a working restore-drill command from a fresh directory.",
    "where": "landing install section and README",
    "test": "npm test -- --grep @claim:install-from-site",
    "sandbox": "fresh temporary directory; clone the source, install with the published command, and run restore-drill --help"
  },
  {
    "id": "free-cli",
    "claim": "The Restore Drill CLI is free and the site has no unavailable purchase link.",
    "where": "README and landing page",
    "test": "npm test -- --grep @claim:free-cli",
    "sandbox": "fresh Chromium context; assert the CLI is described as free and no checkout URL is present"
  },
  {
    "id": "isolated-container",
    "claim": "The CLI runs Postgres with no network or published port and tmpfs database storage.",
    "where": "hero facts, how it works, and README",
    "test": "npm test -- --grep @claim:isolated-container",
    "sandbox": "temporary directory and fake container runtime; assert real binary runtime arguments"
  },
  {
    "id": "data-tmpfs-size",
    "claim": "The CLI accepts a temporary database disk size from 512 MB to 64 GB and records the selected size.",
    "where": "landing install section, CLI help, receipt, and README",
    "test": "npm test -- --grep @claim:data-tmpfs-size",
    "sandbox": "real binary and controlled runtime; assert an 8 GB selection reaches the runtime and receipt, then reject out-of-range values"
  },
  {
    "id": "backup-local",
    "claim": "The backup stays on the machine and is mounted read-only without being changed.",
    "where": "hero facts, boundaries, privacy, and README",
    "test": "npm test -- --grep @claim:backup-local",
    "sandbox": "temporary backup and fake container runtime; compare bytes and assert read-only mount"
  },
  {
    "id": "newer-version",
    "claim": "The CLI catches a newer source dump and an unsupported transaction_timeout setting before restore.",
    "where": "README compatibility notes",
    "test": "npm test -- --grep @claim:newer-version",
    "sandbox": "bundled incompatible Postgres 17.6 sample targeting 15.8; unavailable runtime proves preflight stops first"
  },
  {
    "id": "signed-receipt",
    "claim": "Every started drill writes an HMAC-SHA256 signed JSON receipt, including a preflight failure.",
    "where": "receipt preview, how it works, and README",
    "test": "npm test -- --grep @claim:signed-receipt",
    "sandbox": "temporary directory; create, verify, change, and reject a signed receipt"
  },
  {
    "id": "default-signing-key",
    "claim": "The default signing key is created beside the receipt with mode 0600 on Unix.",
    "where": "README receipt signing section",
    "test": "npm test -- --grep @claim:default-signing-key",
    "sandbox": "temporary directory; omit the key option, inspect the adjacent key permissions, and verify the receipt"
  },
  {
    "id": "schema-readiness",
    "claim": "The CLI checks each required schema and records a distinct pass or failure.",
    "where": "landing workflow, CLI help, demo, receipt, and README",
    "test": "npm test -- --grep @claim:schema-readiness",
    "sandbox": "real binary and controlled runtime; assert both present-schema and missing-schema receipts"
  },
  {
    "id": "cli-demo",
    "claim": "The CLI demo copies the bundled sample into a separate directory and completes a real drill there.",
    "where": "browser replay caption, README, and demo documentation",
    "test": "npm test -- --grep @claim:cli-demo",
    "sandbox": "real binary and controlled runtime; assert copied sample, pass receipt, key, schema/role/table checks, and unchanged source"
  },
  {
    "id": "podman-runtime",
    "claim": "The CLI can run the isolated drill through Podman.",
    "where": "CLI help and README",
    "test": "npm test -- --grep @claim:podman-runtime",
    "sandbox": "real binary with a controlled executable named podman; assert no network, no port, tmpfs, and read-only mount arguments"
  },
  {
    "id": "dump-formats",
    "claim": "The CLI restores plain SQL directly and sends pg_dump archives through pg_restore.",
    "where": "CLI help and README",
    "test": "npm test -- --grep @claim:dump-formats",
    "sandbox": "temporary custom-format header and fake container runtime; assert the real binary invokes pg_restore"
  },
  {
    "id": "ci-mode",
    "claim": "The CLI completes without an interactive prompt and returns a documented exit code.",
    "where": "README",
    "test": "npm test -- --grep @claim:ci-mode",
    "sandbox": "real binary with closed input and a fake container runtime"
  },
  {
    "id": "no-production-url",
    "claim": "The CLI cannot accept a production connection string and contains no network client.",
    "where": "safety boundary and README",
    "test": "npm test -- --grep @claim:no-production-url",
    "sandbox": "real CLI help and parser plus dependency lockfile inspection"
  },
  {
    "id": "no-telemetry",
    "claim": "The CLI has no telemetry or HTTP client.",
    "where": "README",
    "test": "npm test -- --grep @claim:no-telemetry",
    "sandbox": "dependency lockfile inspection plus real CLI option inspection"
  }
]
